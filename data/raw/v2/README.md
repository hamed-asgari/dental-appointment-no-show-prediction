# Version 2 frozen synthetic raw benchmark

These files are fully synthetic and were generated by the Project 2-owned
longitudinal generator. They do not represent real patients, clinical records,
or healthcare operations and are not validated for clinical or operational use.

The committed files were generated from:

- configuration: `configs/v2_synthetic_benchmark.json`
- generator version: `2.0.0`
- root seed: `20260805`
- source command:
  `python -m src.synthetic.export --overwrite`

`v2_synthetic_benchmark.manifest.json` records the configuration hash,
generator-source hashes, named random-stream seeds, table hashes, row counts,
date ranges, status counts, protected evaluation schedule, and dataset
fingerprint.

Handling rules:

- treat the CSV files and manifest as immutable;
- do not edit raw values manually;
- regenerate only through the documented command;
- any generator, configuration, or raw-byte change creates a new data version;
- do not inspect protected 2027 target metrics during model development.

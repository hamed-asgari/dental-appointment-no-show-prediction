# Model artifacts

This directory is reserved for serialized model artifacts created during later project phases. No models exist in v0.1, and this directory does not indicate that model development has started.

Model artifacts are ignored by default because they are generated outputs. Any future decision to version an artifact should be explicit, documented, and reproducible.
